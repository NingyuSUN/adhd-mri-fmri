# Frozen third-party and historical source: preserve exact SHA256-bound bytes.
# Review the immutable snapshot through legacy_manifest.json; new code stays textual.
reproduction/legacy/** -text -diff
# User-configurable inventory helper: retain byte-exact hashes and show reviews.
reproduction/legacy/build_multimodal_manifest.py -text diff
