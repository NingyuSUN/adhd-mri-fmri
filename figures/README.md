# Figures

## Analysis v2 (committed)

| File | Contents |
|---|---|
| `v2_F1_loso_forest.png` | Per-held-out-site ΔAUC for the primary contrast (`full_fusion_structural_mlp` − `full_functional_mlp`) under strict LOSO, one panel per QC cohort, with within-site subject-bootstrap 95% CIs. Shows the cross-site / cross-cohort instability that makes LOSO uninformative here. |
| `v2_F2_cv_bars.png` | Mixed-site CV mean AUC (5 repeats) for cohort `primary`: non-imaging control vs +confounds vs image-only, with repeat-range whiskers. |
| `v2_F3_reliability.png` | Reliability curves for the four report models, cohort `primary`, pooled CV test folds. |

Generated by `notebooks/figures_v2.py` from `results/v2_*.csv`.

## Suggested additional figures (not yet produced)

1. `t1_structural_pipeline.png` — BIDS T1 → QC → slices → CNN → subject prediction.
2. `roi_guided_pipeline.png` — MNI registration → Harvard-Oxford atlas → ROI slice selection.
3. `fmri_graph_pipeline.png` — fMRI → ROI time series → connectivity matrix → graph → GNN.
4. `site_bias_summary.png` — ADHD proportion by site and site-only baseline performance.
5. `ablation_results.png` — Full model vs ablations.
